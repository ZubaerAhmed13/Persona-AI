{
  "name": "persona-ai",
  "version": "3.1.1",
  "private": true,
  "type": "module",
  "description": "Persona AI local-first single-file application hardening build",
  "scripts": {
    "build": "node scripts/build.mjs && node scripts/fix-functional-tags.mjs && node scripts/enforce-hardening-policy.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node scripts/browser-smoke.mjs",
    "test:acceptance": "node scripts/full-browser-acceptance.mjs",
    "test:features": "node scripts/feature-workflow-acceptance.mjs",
    "verify": "node scripts/verify.mjs",
    "check": "npm run build && npm test && npm run verify && npm run test:browser && npm run test:acceptance && npm run test:features"
  },
  "engines": {
    "node": ">=20"
  }
}
